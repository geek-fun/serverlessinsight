version: 0.0.1

provider:
  name: aliyun
  region: cn-chengdu


service: insight-table-poc-mini

tags:
  owner: geek-fun

tables:
  insight_poc_table:
    collection: store_or_instance_id
    name: insight-poc-table
    type: TABLE_STORE_H
    key_schema:
      - name: id
        type: HASH
    attributes:
      - name: id
        type: STRING

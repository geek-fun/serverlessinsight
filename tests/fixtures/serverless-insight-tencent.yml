version: 0.0.1
app: insight-poc-app
provider:
  name: tencent
  region: ap-guangzhou

vars:
  region: ap-guangzhou

stages:
  default:
    region: ${vars.region}

service: insight-poc-tencent

functions:
  test_fn:
    name: test-fn
    code:
      runtime: nodejs18
      handler: index.handler
      path: tests/fixtures/artifacts/artifact.zip
    memory: 512
    timeout: 10

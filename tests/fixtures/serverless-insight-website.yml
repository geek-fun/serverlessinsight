version: 0.0.1
app: insight-bucket-app

provider:
  name: aliyun
  region: cn-chengdu

service: insight-bucket-poc

tags:
  owner: geek-fun

buckets:
  insight_poc_bucket:
    name: insight-poc-bucket
    website:
      code: dist
      index: index.html

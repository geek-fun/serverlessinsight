version: 0.0.1

provider:
  name: aliyun
  region: cn-chengdu


service: insight-table-poc

tags:
  owner: geek-fun

tables:
  insight_poc_table:
    collection:
      name: xxx
      id: abc
    name: insight-poc-table
    type: TABLE_STORE
    network:
      type: 'PRIVATE'
      ingress_rules:
        - TCP:0.0.0.0/0:80
        - TCP:0.0.0.0/0:443
    cu:
      min: 1
      max: 2

